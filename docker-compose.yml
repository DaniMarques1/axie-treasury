version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:3000"                   # map host port 80 → container 3000
    environment:
      - NODE_ENV=production
    restart: unless-stopped
